# Gitlab Import Test
Fetch id of a gitlab project based on name and path of the project.
## Usage
You must be in the project root in order for it to work.

`go run . --path=<path to repository> --name=<name of repository>`
